import random
import turtle


# Da un color aleatorio!
def dar_color_aleatorio():
    return "#%06X" % (random.randint(0, 0xFFFFFF))

# ====================== NO CAMBIE EL CÓDIGO ENCIMA ===========================


if __name__ == '__main__':
    window = turtle.Screen()
    window.bgcolor('white')
    
    # Cree una tortuga nueva que se llama "mi_tortuga"
    
    # Cambie la forma de la tortuga
    
    #Fije la velocidad de su tortuga (0 = más rápido, 1 = más lento, 10 = muy rápido)
    
    #Fije el color de su tortuga con .color('green')
    
    #Use un bucle para repetir el código debajo 50 veces
        
        # Fije el color de su tortuga a un color aleatorio

        # Mueva la tortuga (5*x) píxeles. 'x' es la variable del bucle
        
        # Doble la tortuga (360/7) grados a la derecha (derecha = right)
         
        #Fije el ancho de la tortuga a 'x' (la variable del bucle)
        
    # Compare su pauta con el imagen "EspiralDeTortuga.png". Si los pautas son iguales, esta actividad está completada
    
# ===================== NO CAMBIE EL CÓDIGO DEBAJO ============================
    turtle.done()