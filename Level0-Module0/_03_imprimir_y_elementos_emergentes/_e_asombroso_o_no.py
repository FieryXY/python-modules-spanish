from tkinter import messagebox, simpledialog, Tk
import random

#Traducciones Nuevos
# Aleatorio: Random (rand)
# Número Entero: Integer o Int

# Create an if-main code block, *hint, type main then ctrl+space to auto-complete
    
    # Cree una variable nueva de una pantalla, pantalla = Tk()

    # Esconda la pantalla con la función .withdraw() de la pantalla

    # 1. Crea una variable que es igual a un número positivo que es menos de 4
        # Pista: Fije el valor de la variable a ser igual de random.randInt(0,3) a generar un número aleatorio entre 0 y 3.

    # 2. Imprima su variable a la consola

    # 3. Pídale al usuario algo que el usuario cree que es asombroso
    
    # 4. Si su variable es 0
        # Dígale al usuario que la cosa que puso es asombrosa

    # 5. Si su variable es 1
        # Dígale al usuario que la cosa que puso es decente

    # 6. Si su variable es 2
        # Dígale al usuario que la cosa que puso es decente es aburrida
    
    # 7. Si su variable es 3
        # Cree su propio mensaje
        
    # Use la función .mainloop() con "pantalla"
